# Native Health Clients Placeholder

This directory keeps native health clients as control-plane edge placeholders.

- Android client boundary: `clients/health-android/`
- iOS client boundary: `clients/health-ios/`
- Integration mode: register through `Connectors`, then dispatch through `Jobs`.
- Non-goal: no direct Health runtime integration inside Python gateway default kernel.
